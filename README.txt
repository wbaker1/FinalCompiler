ReadME For Wesley and Cameron's Compiler:

Ensure all files contained in the ZIP are extracted and placed into your Ubuntu home Directory
Open up the terminal and input the following commands:

cd *Directory our folder*
make clean
make parser

This should output the code we included in our videos, we have wiped the MIPSCode.asm and the IRCode.r